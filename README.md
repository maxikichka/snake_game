My first decent project with C programming language. 

Must run inlinux terminal, it uses curses.h module for controllign the terminal. 

Compile with:
gcc -o fileName fileName.c -lncurses

Run with:
./fileName
